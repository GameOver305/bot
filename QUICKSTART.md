# دليل الاستخدام السريع 🚀

## خطوات البدء السريع

### 1. التثبيت
```bash
# تثبيت الحزم
npm install

# إنشاء ملف .env
cp .env.example .env
```

### 2. إعداد البوت

**أ. في Discord Developer Portal:**
1. إنشاء Application جديد
2. تفعيل Bot
3. تفعيل Intents المطلوبة
4. نسخ Token

**ب. في ملف .env:**
```env
DISCORD_TOKEN=توكن_البوت_هنا
OWNER_ID=معرف_المستخدم_الخاص_بك
CLIENT_ID=معرف_البوت
GUILD_ID=معرف_السيرفر_للاختبار
```

### 3. التشغيل
```bash
npm start
```

## الأوامر الأساسية

### أوامر الإعداد (للمرة الأولى)
```
/setowner       - تعيين المالك (مرة واحدة فقط)
/addadmin @user - إضافة مشرف
```

### الأمر الرئيسي
```
/panel  أو  /لوحة
```
هذا الأمر يفتح لوحة التحكم الكاملة بالأزرار

## القوائم المتاحة

### 1️⃣ نظام الحجوزات
- 🏗️ مواعيد البناء
- 🔬 مواعيد الأبحاث
- ⚔️ مواعيد التدريب

**إضافة حجز:**
1. اختر نوع الحجز
2. اضغط "إضافة حجز"
3. أدخل التاريخ: `2024-02-15`
4. أدخل المدة: `3` (أيام)
5. ملاحظات اختيارية

### 2️⃣ نظام التحالف
- عرض معلومات التحالف
- إدارة الأعضاء (R4, R5 فقط)
- الرتب: R5 (القائد) → R1

### 3️⃣ الإعدادات
- تغيير اللغة (عربي/English)
- تفعيل/تعطيل التذكيرات

### 4️⃣ الصلاحيات
- عرض المالك والمشرفين
- متاح للمالك فقط

## التذكيرات التلقائية ⏰

عند إضافة حجز، ستصلك تذكيرات في:
- ⏰ قبل 24 ساعة
- ⏰ قبل 6 ساعات
- ⏰ قبل 3 ساعات
- ⏰ قبل ساعة واحدة

**ملاحظة:** يمكنك تعطيل التذكيرات من الإعدادات

## نصائح مهمة 💡

### ✅ افعل
- استخدم تنسيق التاريخ الصحيح: `YYYY-MM-DD`
- تحقق من عدم التعارض قبل الحجز
- فعّل Developer Mode في Discord لنسخ المعرفات

### ❌ لا تفعل
- لا تشارك Token البوت مع أحد
- لا تحفظ `.env` في Git
- لا تنسى تفعيل Intents المطلوبة

## مثال عملي 📝

### سيناريو: إضافة حجز بناء

1. المستخدم يكتب: `/panel`
2. يضغط على: `📅 نظام الحجوزات`
3. يختار: `🏗️ مواعيد البناء`
4. يضغط: `➕ إضافة حجز`
5. يملأ النموذج:
   - تاريخ البداية: `2024-02-20`
   - المدة: `5`
   - ملاحظات: `بناء القلعة المستوى 25`
6. يرسل النموذج

**النتيجة:**
- ✅ يضاف الحجز من 20 فبراير إلى 25 فبراير
- 🔔 يتم جدولة 4 تذكيرات تلقائية
- 📋 يظهر الحجز في قائمة الحجوزات

## الأخطاء الشائعة وحلولها 🔧

### "البوت غير متصل"
```bash
# تحقق من Token
echo $DISCORD_TOKEN

# أعد تشغيل البوت
npm start
```

### "الأوامر لا تظهر"
- انتظر حتى 1 ساعة للأوامر العامة
- أو استخدم GUILD_ID للاختبار الفوري

### "لا يمكن إرسال رسائل خاصة"
- تحقق من إعدادات الخصوصية في Discord
- تأكد من أن المستخدم لم يحظر البوت

### "خطأ في التاريخ"
```
❌ خطأ: 15-02-2024
✅ صحيح: 2024-02-15
```

## هيكل البيانات 📊

### ملفات JSON المخزنة
```
data/
├── users.json        # تفضيلات المستخدمين
├── bookings.json     # جميع الحجوزات
├── alliance.json     # معلومات التحالف
├── permissions.json  # الصلاحيات
└── reminders.json    # إعدادات التذكيرات
```

## الصيانة والتنظيف 🧹

### تنظيف تلقائي
- يحدث يومياً في منتصف الليل
- يحذف الحجوزات المنتهية
- لا يتطلب تدخل يدوي

### تنظيف يدوي (إذا لزم الأمر)
```bash
# احذف ملفات البيانات
rm data/*.json

# أعد تشغيل البوت (سيعيد إنشاء الملفات)
npm start
```

## الأداء والسعة 📈

**السعة الحالية:**
- ✅ آلاف الحجوزات
- ✅ مئات المستخدمين
- ✅ عشرات السيرفرات
- ⚡ استجابة فورية

**للسعات الأكبر:** انظر قسم "التحديثات المستقبلية" في README.md

## الدعم 📞

**لديك سؤال؟**
1. راجع [README.md](README.md) للتوثيق الكامل
2. تحقق من قسم "حل المشاكل"
3. افتح Issue على GitHub

---

**نصيحة أخيرة:** ابدأ بسيرفر اختبار قبل الاستخدام الفعلي! 🎯
